# Global UI settings
DEFAULT_WIDTH = 1280
DEFAULT_HEIGHT = 720
BUTTON_SIZE = (300, 50)  # Width, height for all buttons
BUTTON_BG_COLOR = (64, 64, 64)  # Dark gray background for buttons
PADDING = 25  # Padding between elements (in default resolution)
TEXT_SETTINGS = {
    "title": {"font_scale": 1, "thickness": 2},
    "button": {"font_scale": 1, "thickness": 2},
    "tracking": {"font_scale": 0.6, "thickness": 1}  # Reduced font scale and thickness for 720p
}